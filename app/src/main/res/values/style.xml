<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/color_medium_purple</item>
        <item name="colorPrimaryVariant">@color/color_dark_turquoise</item>
        <item name="colorOnPrimary">@color/color_white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!--Bottom navigation-->
    <style name="TextBottomNavigationStyle">
        <item name="fontFamily">@font/roboto</item>
        <item name="android:textColor">@color/color_orange_turmeric</item>
    </style>

    <!--Tab layout-->
    <style name="TabLayoutTextAppearance" parent="TextAppearance.AppCompat.Widget.ActionBar.Title.Inverse">
        <item name="android:textSize">@dimen/dp_18</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>
